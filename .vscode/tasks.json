{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile and Push",
      "type": "shell",
      "command": "echo \"Compiling: ${fileBasename}\"; javac -d out \"${file}\" || exit 1; git add .; if ! git diff --cached --quiet; then git commit -m \"Added : ${fileBasename}\"; git push; else echo \"No changes to commit.\"; fi",
      "options": {
        "shell": {
          "executable": "C:\\Program Files\\Git\\bin\\bash.exe",
          "args": ["-c"]
        }
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
